
export interface ITeacherReviewCreateries {
    freebie: number | null,
    strictness: number | null,
    smartless: number | null,
    friendliness: number | null,
    experienced: number | null,
}